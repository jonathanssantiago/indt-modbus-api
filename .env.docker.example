# Ambiente Docker - Arquivo de exemplo
# Copie este arquivo para .env.docker e ajuste as configurações

NODE_ENV=production
PORT=3003

# Database (Docker PostgreSQL)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=modbus_db

# Modbus Configuration
MODBUS_HOST=host.docker.internal
MODBUS_PORT=5020
READING_INTERVAL=5000

# Logs
LOG_LEVEL=info
